name: Build GOST ARMv7

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Check source
        run: |
          git status
          git log -1
          go version

      - name: Build ARMv7
        env:
          GOOS: linux
          GOARCH: arm
          GOARM: '7'
          CGO_ENABLED: '0'
        run: |
          go build -o gost ./cmd/gost
          file gost

      - name: Package
        run: |
          tar -czf gost-linux-armv7.tar.gz gost

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: gost-linux-armv7
          path: gost-linux-armv7.tar.gz
